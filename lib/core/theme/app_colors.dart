import 'package:flutter/material.dart';

class AppColors {
  // ألوان مأخوذة من ملف التصميم (SVG)
  static const Color primary = Color(
    0xFF224F78,
  ); // اللون الرئيسي المستخدم في التصميم
  static const Color primaryDark = Color.fromARGB(253, 15, 9, 47);
  static const Color accent = Color(0xFFF75555); // لون ثانوي (أحمر) لو احتجته
  static const Color gold = Color(0xFFE0B168); // لون ذهبي في التصميم
  static const Color background = Color(0xFFF5F5F5); // لون خلفية عام خفيف
  static const Color surface = Color(0xFFECECEB); // ظل/سطح فاتح
  static const Color textDark = Color(0xFF3D3D3D);
  static const Color textLight = Colors.white;

  // ألوان الصفحة
  static const Color primaryColor = Color(0xFF224F78); // أزرق عميق
  static const Color accentColor = Color(0xFFE0B168); // ذهبي دافئ
  static const Color bgColor = Color(0xFFFAFAFA);
  static const Color surfaceLight = Color(0xFFECECEB);
  static const Color textPrimary = Color(0xFF212121);

  static const Color bgTop = Color(0xFF244C7C);

  static const Color darkText = Color(0xFF484C52);
  static const Color lightGray = Color(0xFFF5F5F5);
  static const Color pageBg = Color(0xFFFAFAFA);
}
